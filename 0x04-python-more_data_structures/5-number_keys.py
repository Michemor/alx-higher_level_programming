#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    returns the number of keys contained in a dictionary
    Input: a_dictionary
    Output: number of keys
    """
    key_list = dict(a_dictionary)
    return (len(key_list))
